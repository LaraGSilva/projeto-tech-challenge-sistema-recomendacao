# Exemplos para o modelo PyTorch
neumf:
  n_factors: 64
  mlp_hidden_layers: [128, 64, 32]
  learning_rate: 0.001
  batch_size: 1024
  epochs: 20
  dropout: 0.2

# Exemplos para o KNN
knn:
  n_neighbors: 20
  metric: "cosine"